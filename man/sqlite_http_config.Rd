% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/httpvfs_config.R
\name{sqlite_http_config}
\alias{sqlite_http_config}
\title{Configure experimental HTTP VFS behavior (optional)}
\usage{
sqlite_http_config(
  cache_size_mb = NULL,
  prefetch_pages = NULL,
  fallback_full_download = NULL
)
}
\arguments{
\item{cache_size_mb}{Integer megabytes for in-memory page cache. \code{NULL} leaves unchanged.}

\item{prefetch_pages}{Integer pages to prefetch ahead. \code{NULL} leaves unchanged.}

\item{fallback_full_download}{Logical; if \code{TRUE}, allow full-download fallback when Range is not available. \code{NULL} leaves unchanged.}
}
\value{
A named list of previous values (in R types).
}
\description{
Set conservative, process-wide options for the experimental HTTP/HTTPS VFS.
These values are read on first open of a remote database in the current process.
}
\details{
Options are stored in environment variables so they also affect C-level code:
\itemize{
\item \code{RSQLITE_HTTP_CACHE_MB}: In-memory page cache size in megabytes (default 4).
\item \code{RSQLITE_HTTP_PREFETCH_PAGES}: Prefetch this many pages ahead (default 0).
\item \code{RSQLITE_HTTP_FALLBACK_FULLDL}: If \code{TRUE} (default), fall back to full download when the server does not support HTTP Range; if \code{FALSE}, open will fail.
}
}
\examples{
old <- sqlite_http_config(cache_size_mb = 8, prefetch_pages = 1, fallback_full_download = TRUE)
on.exit(do.call(sqlite_http_config, old), add = TRUE)
# ... connect with sqlite_remote() ...
}
