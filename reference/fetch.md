# Fetch

A shortcut for
[`dbFetch`](https://dbi.r-dbi.org/reference/dbFetch.html)`(res, n = n, row.names = FALSE)`,
kept for compatibility reasons.

## Usage

``` r
# S4 method for class 'SQLiteResult'
fetch(res, n = -1, ...)
```
